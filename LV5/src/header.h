#ifndef HEADER_H
#define HEADER_H
    
    int* allocMatrix(int*, int);
    void populateMatrix(int*, int);
    int biggestEvenNum(int*, int);
    int* newMatrix(int*, int, int);
    void printMatrix(int*, int);
    void eraseMatrix(int*);
    
#endif